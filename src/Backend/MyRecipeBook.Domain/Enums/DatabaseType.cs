namespace MyRecipeBook.Domain.Enums
{
    //classe enum para mapear lista de enumeradores que serão utilizados em outras classes
    public enum DatabaseType
    {
        Sql = 0,
        MySql = 1,
    }
}
